angular.module('imageBrowserService', []).factory('$imageBrowserService', ["$api", function($api) {

	APPLICATION_ROOT_FOLDER = "appenza";
    var service = {};
    service.browse = function() {
    }
    return service;
}]);
